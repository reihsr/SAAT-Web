import { DictionaryTreeNode } from './dictionary-tree-node';

describe('DictionaryTreeNode', () => {
  it('should create an instance', () => {
    expect(new DictionaryTreeNode()).toBeTruthy();
  });
});
